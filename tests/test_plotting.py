# test plotting

# test that calling plotting function opens plot (make sure this also works if machine has no graphical output)

# test that if save path is given file is saved (and delete afterwards)